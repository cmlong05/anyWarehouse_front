# 在bun 1.3版本中等待mysql的API

# 数据库换 PostgreSQL 也可直接换API

# 对于物品侧栏中的重复数据，应该也保留，但是去掉链接属性
# 同理，其他展示也做类似变更

# sidebar 应该独立出来，使用模板，然后以灌入数据的形式展现
    思路应该是：
        现有模板 与 布局
        后 灌入数据即可，模板中留有冗余，应对不同的数据
    优势：
        1. 结构更清晰，减少重复代码
        2. 减少前端页面显示不必整体刷新
        
# 增加修改数据功能

# 修改static/shared.css为使用tailwindcss
    修改模板中的class

# 设置图片服务器

# 货币单位完善

# 物品详情页面添加编辑按钮 删除按钮（验证是否无库存）（验证由后端完成）
# 类别中添加新建物品按钮
# 类别中添加新建分类按钮 编辑分类、删除分类（验证无子分类）（验证由后端完成）

# 每个容器中添加 编辑容器按钮 删除容器（验证是否为空）（验证由后端完成） 添加平行容器和子容器 两个选项

# 国际化：支持多种语言

# 完成物品的分类组成功能（后端配合）

# 顶级容器 与 无父容器即为根容器，是否重复？
# 还是说，加上顶级容器的选项，会更加方便排序？
# 需要后端支持